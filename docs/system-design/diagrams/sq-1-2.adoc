@startuml
actor User
participant UI
participant Communication as Comm
participant MonitoringController as MC

User -> UI : Enter plant details

  UI -> Comm : Send configuration
  Comm -> MC : ReceiveConfig()
  MC -> MC : UpdateConfiguration()
  MC --> Comm : Failed
  Comm --> UI : Failed
  UI --> User : Failed to add plant

@enduml
